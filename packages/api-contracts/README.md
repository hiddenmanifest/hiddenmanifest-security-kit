# @hiddenmanifest/api-contracts

Public request and response contract types for Hidden Manifest.

This package documents the wire shapes used by the browser and hosted services without exposing production server handlers, routing strategy, Redis storage, broadcaster implementation, or DEX adapters.

