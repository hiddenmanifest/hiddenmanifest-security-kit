# @hiddenmanifest/railgun-browser

Browser-only RAILGUN boundary types and worker client shell for Hidden Manifest.

This package provides public interfaces for browser wallet creation/loading, viewing keys, balance reads, shield/unshield/transfer transaction generation, and private swap transaction generation.

It intentionally excludes production routing, 0x adapters, commercial service logic, broadcaster endpoints, Redis, and hosted server code.

