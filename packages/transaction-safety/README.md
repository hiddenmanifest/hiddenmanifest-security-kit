# @hiddenmanifest/transaction-safety

Public transaction safety helpers for Hidden Manifest.

This package provides generic, adapter-free helpers for validating transaction intent, token amounts, addresses, chain IDs, native tokens, and ERC20 allowance decisions.

It does not include wallet provider adapters, production routing, broadcaster selection, or hosted service code.

